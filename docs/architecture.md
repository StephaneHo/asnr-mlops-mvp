# Architecture

> Document en construction. Sera complété avec :
>
> - Schéma de flux de données (ingestion → parsing → classification → extraction → stockage).
> - Schéma de flux des modèles (versions, registry MLflow, déploiement).
> - Diagramme Mermaid des composants (FastAPI, Celery, Postgres+pgvector, MinIO, Redis, Ollama).
> - Choix de souveraineté (CPU-only, on-prem, zéro dépendance cloud).

Voir aussi : [ADRs](adr/) et [Model cards](model_cards/).
